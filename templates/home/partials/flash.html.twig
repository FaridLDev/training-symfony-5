{% for label, messages in app.flashes %}
  <div class="container">
    <div class="alert alert-{{ label }} my-3">
      {% for message in messages %}
        <p>
          {{ message|raw }}
        </p>
      {% endfor %}
    </div>
  </div>
{% endfor %}